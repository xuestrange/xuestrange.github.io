# Jekyll build output and local caches.
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler local install directory, if you ever use `bundle install --path vendor/bundle`.
vendor/bundle/

# Local Cloudflare Worker state and secrets.
analytics-worker/.wrangler/
analytics-worker/.dev.vars*
analytics-worker/.env*
analytics-worker/.admin-token
!analytics-worker/.dev.vars.example
!analytics-worker/.env.example
